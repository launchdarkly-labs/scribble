# scribble

Local-first annotation layer for HTML documents, so humans and agents can collaborate on rich artifacts the same way Hunk lets them collaborate on diffs.

See `notes/00-initial-design.html` for the current thinking.
